测试方式
```
java -jar ~/bundletool-all-1.5.0.jar build-apks --local-testing --bundle app-debug.aab --output app-debug.apks

```


```
 java -jar ~/bundletool-all-1.5.0.jar install-apks --apks app-debug.apks 
```